{
  "name": "ai-officer-poc",
  "version": "1.0.0",
  "description": "Server for serving index.html file",
  "main": "server.js",
  "scripts": {
    "start": "pm2 start server.js --name 'server'",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
